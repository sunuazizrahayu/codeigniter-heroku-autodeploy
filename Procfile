##### if your document root and project root in the same directory
web: vendor/bin/heroku-php-nginx -C heroku.nginx.conf



##### if your document root on public directory 
# web: vendor/bin/heroku-php-nginx -C heroku.nginx.conf public